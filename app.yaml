command: ["streamlit", "run", "app.py", "--server.port", "8000", "--server.address", "0.0.0.0"]

env:
  - name: SUPERVISOR_ENDPOINT
    value: "REPLACE-WITH-YOUR-SUPERVISOR-ENDPOINT"
  - name: AUTH_MODE
    value: "auto"
