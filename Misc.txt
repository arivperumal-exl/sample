When a sub-agent (Genie) returns a chart or visualization, preserve and display 
it directly in your final response rather than converting the result into a table. 
If the underlying data is well suited to a chart (trends, comparisons, distributions), 
render it as a chart rather than reformatting it as text/markdown.
