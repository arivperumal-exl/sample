best_thresh_idx = int(np.argmax(f1s))
best_t = float(thresholds[best_thresh_idx])
pred_labels = (arr_s >= best_t).astype(int)

metrics["img_f1"]        = float(max(f1s))
metrics["img_threshold"] = best_t
metrics["img_precision"] = float(precision_score(arr_l, pred_labels, zero_division=0))
metrics["img_recall"]    = float(recall_score(arr_l, pred_labels, zero_division=0))
