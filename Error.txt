# Draw bounding box around most anomalous region
bbox = self.get_bounding_box(anomaly_map, (H, W))
if bbox is not None:
    x, y, w, h = bbox
    overlay = cv2.rectangle(overlay.copy(), (x, y), (x+w, y+h), (255, 0, 0), 2)
    # Label the box
    cv2.putText(overlay, "Defect", (x, y - 10),
                cv2.FONT_HERSHEY_SIMPLEX, 0.6, (255, 0, 0), 2)
